#include <iostream>
using namespace std;

void columnSum(int A[][10], int r, int c) {
    for (int j = 0; j < c; j++) {
        int sum = 0;
        for (int i = 0; i < r; i++) {
            sum += A[i][j];
        }
        cout << "Sum of column " << j + 1 << " = " << sum << endl;
    }
}

int main() {
    int r, c;
    cin >> r >> c;

    int A[10][10];

    for (int i = 0; i < r; i++) {
        for (int j = 0; j < c; j++) {
            cin >> A[i][j];
        }
    }

    columnSum(A, r, c);

    return 0;
}
